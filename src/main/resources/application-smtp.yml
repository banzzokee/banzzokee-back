spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${GOOGLE_SMTP_EMAIL}
    password: ${GOOGLE_SMTP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true