server:
  port: 8080

spring:
  profiles:
    include:
      - jpa
      - s3
      - smtp
      - redis
      - jwt
      - elasticsearch

logging:
  level:
    org.springframework.data.elasticsearch.client:
      WIRE: TRACE